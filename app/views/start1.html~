<div align="center">
<br/>
<h1 ng-model="user">Welcome <br/> {{user.name}}</h1>
<br/>
<br/>
<h1>What are you in the mood for?</h1>
<h2>Select the genres you would like to watch</h2>
<br/>
<br/>


<div class="container">
    <div align="center" class="btn-group col-xs-12" data-toggle="buttons">
        <label ng-repeat="genre in genres()" class="btn btn-default">
            <input class = "active" ng-click ="addGenre(genre)" type="checkbox" name="{{genre.name}}" value="{{genre.id}}">{{genre.name}}
        </label>
    </div>
</div>

<br/>
<br/>
<br/>
<a ng-click="search(query)" href="#/start2" class="btn btn-warning btn-lg">NEXT</a>
</div>
